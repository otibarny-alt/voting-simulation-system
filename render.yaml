services:
  - type: web
    name: training-e-ballot-prototype
    env: python
    buildCommand: pip install -r requirements.txt && python -m whitenoise.compress static
    startCommand: gunicorn app:app --workers 2 --threads 8 --timeout 60 --keep-alive 5
